# Python
__pycache__/
*.py[cod]
*.so
.Python
build/
dist/
*.egg-info/

# Virtual environments
.venv/
venv/
env/

# Data folders generated by preprocess.py
Data/
Data_preprocessed/
Data_trained/
Data_sampled/
Data_environment/
SVG_model/
Plots/
Reward_weights/

# A copy of the data tree kept inside the repo to run the golden cases
# (tools/golden/run_cases.py --data-root Git_version_final). The entries above
# are anchored to the repo root, so they do not cover it when it is nested.
Git_version_final/

# Output tree for a full pipeline run: inputs are symlinks to the source data,
# everything else is generated. Currently invisible to git only because its
# contents happen to match the *.pkl / *.pt rules above.
run_full/

# Model weights and datasets
*.pkl
*.pt
*.pth
*.ckpt
*.csv
*.geojson

# Notebooks
.ipynb_checkpoints/

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db
